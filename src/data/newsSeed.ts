import { NewsPost } from '../types';

/**
 * Institutional News & Bulletins Registry.
 * Demo announcements have been completely cleared.
 * Real-time press updates are published via the Web Management portal and stored in MongoDB.
 */
export const initialNewsSeed: NewsPost[] = [];
