import { ResultRecord } from '../types';

/**
 * Examination Results Registry.
 * Official marks and grades are recorded by the Administrator through the
 * Results Management panel and synchronized with the backend MongoDB database.
 */
export const initialResultsSeed: ResultRecord[] = [];
